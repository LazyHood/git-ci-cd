name: CI/CD Pipeline

on:
  push:
    branches: [ main, master, development, release ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'
      
      - name: Display commit info
        run: |
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
          echo "Author: ${{ github.actor }}"
      
      - name: Add platform to Gemfile.lock
        run: bundle lock --add-platform x86_64-linux
      
      - name: Install dependencies
        run: bundle install
      
      - name: Run RSpec tests
        run: bundle exec rspec
      
      - name: Success message
        run: echo "All tests passed!"

  generate-docs:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'
      
      - name: Generate documentation
        run: |
          mkdir -p docs
          echo "# Автоматически сгенерированная документация" > docs/AUTOGEN.md
          echo "" >> docs/AUTOGEN.md
          echo "**Дата генерации:** $(date)" >> docs/AUTOGEN.md
          echo "**Коммит:** ${{ github.sha }}" >> docs/AUTOGEN.md
          echo "**Ветка:** ${{ github.ref_name }}" >> docs/AUTOGEN.md
          echo "" >> docs/AUTOGEN.md
          echo "## Статистика проекта" >> docs/AUTOGEN.md
          echo "" >> docs/AUTOGEN.md
          echo "- Всего файлов Ruby: $(find . -name '*.rb' | wc -l)" >> docs/AUTOGEN.md
          echo "- Всего строк кода: $(find . -name '*.rb' -exec wc -l {} + | tail -1 | awk '{print $1}')" >> docs/AUTOGEN.md
          echo "" >> docs/AUTOGEN.md
          echo "## Структура файлов" >> docs/AUTOGEN.md
          echo "" >> docs/AUTOGEN.md
          echo '```' >> docs/AUTOGEN.md
          tree -L 2 -I 'vendor|.git' || ls -R >> docs/AUTOGEN.md
          echo '```' >> docs/AUTOGEN.md
      
      - name: Commit documentation
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add docs/AUTOGEN.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "📝 Auto-update documentation [skip ci]" && git push)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  merge-to-main:
    needs: [test, generate-docs]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/development' && github.event_name == 'push'
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Setup Git
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
      
      - name: Merge development to main
        run: |
          git fetch origin main
          git checkout main
          git merge --no-ff development -m "🔀 Auto merge development to main [skip ci]"
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}